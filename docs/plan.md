# Scuta Implementation Plan

See the full plan in the platform-core conversation history.

## Phases

1. **Repository Setup** (this phase) — structure, tooling, CI, skeleton code
2. **Feature Implementation** — registry, state, github, installer, auth
3. **api-gen Repo** — Go rewrite of platform-core's Python API generator
